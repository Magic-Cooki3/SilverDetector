# Build output - ./build.sh regenerates both, and bin/silverdetector builds on first run.
build/
silverdetector.jar

# Backups written by `--update` before it swaps in new files.
.backups/

# Editor and OS noise
*.swp
*~
.idea/
.vscode/
